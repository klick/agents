<style>
  .border.rounded,
  pre.border.rounded {
    border-radius: 3px;
  }

  .agents-page-intro {
    margin: 0 0 16px;
    color: #596673;
    max-width: 920px;
  }

  .agents-page-shell {
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;
  }

  .agents-page-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
    text-align: left;
    margin: 12px 0 24px;
  }

  .agents-page-header__copy {
    min-width: 0;
    flex: 1 1 auto;
  }

  .agents-page-header__actions {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    flex: 0 0 auto;
  }

  .agents-page-header h1 {
    margin: 0;
  }

  .agents-page-header .agents-page-intro {
    margin: 6px 0 0;
  }

  .agents-layout-stack > * + * {
    margin-top: 0;
  }

  .agents-layout-stack > :not(hr) + :not(hr) {
    margin-top: 16px;
  }

  .agents-layout-stack > hr {
    margin: 16px -24px;
    border: 0;
    border-top: 1px solid #d7dde3;
  }

  .agents-grid {
    display: grid;
    gap: 12px;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  }

  .agents-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    align-items: center;
  }

  .agents-metric-grid {
    display: grid;
    gap: 12px;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  }

  .agents-metric {
    border: 1px solid #d7dde3;
    border-radius: 3px;
    padding: 10px 12px;
    background: #fbfcfd;
  }

  .agents-metric--good {
    border-color: #9fd8b5;
    background: #f3fcf7;
  }

  .agents-metric--good .value {
    color: #1c6e3b;
  }

  .agents-metric--warn {
    border-color: #f2d58b;
    background: #fffbef;
  }

  .agents-metric--warn .value {
    color: #9a6700;
  }

  .agents-metric--bad {
    border-color: #efb5b5;
    background: #fff5f5;
  }

  .agents-metric--bad .value {
    color: #b42318;
  }

  .agents-metric .label {
    font-size: 12px;
    color: #5f6d7c;
    margin-bottom: 4px;
  }

  .agents-metric .value {
    font-size: 20px;
    line-height: 1.2;
    font-weight: 600;
    color: #18212a;
  }

  .agents-metric-strip-wrap {
    overflow-x: auto;
    margin-top: 0;
    background: #ffffff;
    width: 100%;
    display: flex;
    justify-content: center;
  }

  .agents-metric-strip {
    display: flex;
    flex-wrap: nowrap;
    justify-content: center;
    gap: 0;
    width: max-content;
    min-width: 100%;
    max-width: none;
    flex: 0 0 auto;
    background: #ffffff;
  }

  .agents-metric-strip .agents-metric-strip-card {
    border: 0;
    border-top: 1px solid #c8d0da;
    border-right: 1px solid #c8d0da;
    border-bottom: 1px solid #c8d0da;
    padding: 10px 12px 8px;
    border-radius: 0;
    min-height: 70px;
    background: #ffffff;
    box-shadow: none;
    display: flex;
    flex: 0 0 150px;
    flex-direction: column;
    justify-content: flex-start;
    border-radius: 0;
  }

  .agents-metric-strip .agents-metric-strip-card:first-child {
    border-left: 1px solid #c8d0da;
    border-top-left-radius: 3px;
    border-bottom-left-radius: 3px;
  }

  .agents-metric-strip .agents-metric-strip-card:last-child {
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
  }

  .agents-metric-strip .agents-metric-strip-card .label {
    font-size: 13px;
    line-height: 1.3;
    color: inherit;
    margin: 0 0 6px;
    white-space: nowrap;
  }

  .agents-metric-strip .agents-metric-strip-card .value {
    font-size: 20px;
    line-height: 1;
    font-weight: 600;
    color: inherit;
    margin: 0;
  }

  .agents-metric-strip .agents-metric-strip-card.agents-metric--good {
    background: #ffffff;
    box-shadow: none;
    border-color: #c8d0da;
  }

  .agents-metric-strip .agents-metric-strip-card.agents-metric--warn {
    background: #fffbef;
    box-shadow: none;
    border-color: #c8d0da;
  }

  .agents-metric-strip .agents-metric-strip-card.agents-metric--bad {
    background: #f8e2e2;
    box-shadow: none;
    border-color: #c8d0da;
  }

  .agents-metric-strip .agents-metric-strip-card.agents-metric--good .value,
  .agents-metric-strip .agents-metric-strip-card.agents-metric--warn .value,
  .agents-metric-strip .agents-metric-strip-card.agents-metric--bad .value {
    color: inherit;
  }

  @media (max-width: 767px) {
    .agents-page-header {
      flex-direction: column;
    }

    .agents-page-header__actions {
      width: 100%;
    }

    .agents-metric-strip-wrap {
      overflow-x: visible;
    }

    .agents-metric-strip {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 0;
      width: 100%;
      min-width: 0;
      max-width: none;
    }

    .agents-metric-strip .agents-metric-strip-card {
      min-width: 0;
      flex-basis: auto;
      border-top: 1px solid #c8d0da;
      border-right: 1px solid #c8d0da;
      border-bottom: 0;
      border-left: 1px solid #c8d0da;
      border-radius: 0;
    }

    .agents-metric-strip .agents-metric-strip-card:first-child {
      border-top-left-radius: 3px;
      border-top-right-radius: 3px;
      border-bottom-left-radius: 0;
      border-bottom-right-radius: 0;
    }

    .agents-metric-strip .agents-metric-strip-card:last-child {
      border-bottom: 1px solid #c8d0da;
      border-top-right-radius: 0;
      border-bottom-left-radius: 3px;
      border-bottom-right-radius: 3px;
    }
  }

  .agents-two-col {
    display: grid;
    gap: 16px;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  }

  .agents-link-list {
    margin: 0;
    padding-left: 18px;
  }

  .agents-link-list li + li {
    margin-top: 4px;
  }

  .agents-subtle {
    color: #5f6d7c;
    font-size: 13px;
  }

  .agents-kicker {
    display: block;
    margin-bottom: 6px;
    font-size: 12px;
    font-weight: 600;
    color: #5f6d7c;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .agents-advanced > summary {
    cursor: pointer;
    user-select: none;
  }

  .agents-advanced pre {
    margin-top: 8px;
  }

  table.data tbody tr:last-child td,
  table.data tbody tr:last-child th {
    border-bottom: 0 !important;
  }

  table.data > thead > tr > th {
    border-bottom: 0 !important;
  }

</style>
