{
  "name": "agents-plugin-docs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy": "npm run docs:build && ./scripts/deploy-docs.sh",
    "docs:deploy:dry": "npm run docs:build && DRY_RUN=1 ./scripts/deploy-docs.sh"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
